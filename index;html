<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Conversor de Peças por Peso Teórico</title>
  <link rel="stylesheet" href="style.css" />
</head>
<body>
  <header class="topbar">
    <h1>Conversor de Peças por Peso (kg/m)</h1>
  </header>

  <main class="container">
    <!-- CADASTRO DE MATERIAIS -->
    <section class="card">
      <div class="card-head">
        <h2>Cadastro de Materiais</h2>
        <p class="muted">Informe <strong>Nome</strong> e <strong>Peso por metro (kg/m)</strong>. Os dados ficam salvos no navegador.</p>
      </div>

      <form id="material-form" class="grid">
        <div class="field">
          <label for="matName">Nome do material</label>
          <input id="matName" type="text" placeholder="Ex.: Barra 1” Aço 1020" required />
        </div>
        <div class="field">
          <label for="matPpm">Peso por metro (kg/m)</label>
          <input id="matPpm" type="number" inputmode="decimal" step="0.0001" min="0" placeholder="Ex.: 2.0000" required />
        </div>
        <div class="field actions">
          <button type="submit" class="btn primary">Adicionar</button>
          <button type="button" id="btnClear" class="btn">Limpar</button>
        </div>
      </form>

      <div class="table-wrap">
        <table class="table" id="material-table">
          <thead>
            <tr>
              <th>Nome</th>
              <th>Peso (kg/m)</th>
              <th class="center">Ações</th>
            </tr>
          </thead>
          <tbody><!-- preenchido via JS --></tbody>
        </table>
      </div>
    </section>

    <!-- SELEÇÃO + CÁLCULO ÚNICO -->
    <section class="card">
      <div class="card-head">
        <h2>Cálculo Rápido (Material Selecionado)</h2>
      </div>

      <div class="grid grid-4">
        <div class="field">
          <label for="selMaterial">Material</label>
          <select id="selMaterial"></select>
        </div>
        <div class="field">
          <label for="inComprimento">Comprimento (m)</label>
          <input id="inComprimento" type="number" inputmode="decimal" step="0.0001" min="0" placeholder="Ex.: 12" />
        </div>
        <div class="field">
          <label for="inPecas">Peças (unid.)</label>
          <input id="inPecas" type="number" inputmode="numeric" step="1" min="0" placeholder="Ex.: 5" />
        </div>
        <div class="field">
          <label>&nbsp;</label>
          <button id="btnCalcUnico" class="btn primary block">Calcular</button>
        </div>
      </div>

      <div class="result" id="resUnico">
        <div><span>Peso por metro:</span><strong id="ppmView">—</strong></div>
        <div><span>Peso do comprimento:</span><strong id="pesoComprimentoView">—</strong></div>
        <div><span>Peso total (peças):</span><strong id="pesoTotalView">—</strong></div>
      </div>
    </section>

    <!-- FARDOS -->
    <section class="card">
      <div class="card-head">
        <h2>Cálculo por Fardos (mesmo material)</h2>
        <p class="muted">Informe quantos fardos você quer calcular. Para cada fardo, preencha comprimento (m) e peças.</p>
      </div>

      <div class="grid grid-4">
        <div class="field">
          <label for="selMaterialFardo">Material</label>
          <select id="selMaterialFardo"></select>
        </div>
        <div class="field">
          <label for="inQtdFardos">Qtd. de fardos</label>
          <input id="inQtdFardos" type="number" step="1" min="0" placeholder="Ex.: 5" />
        </div>
        <div class="field">
          <label>&nbsp;</label>
          <button id="btnGerarFardos" class="btn">Gerar linhas</button>
        </div>
        <div class="field">
          <label>&nbsp;</label>
          <button id="btnLimparFardos" class="btn danger">Limpar fardos</button>
        </div>
      </div>

      <div class="table-wrap">
        <table class="table" id="fardos-table">
          <thead>
            <tr>
              <th>#</th>
              <th>Comprimento (m)</th>
              <th>Peças</th>
              <th>Peso do fardo (kg)</th>
            </tr>
          </thead>
          <tbody><!-- linhas via JS --></tbody>
          <tfoot>
            <tr>
              <td colspan="3" class="right"><strong>Total</strong></td>
              <td><strong id="fardosTotal">0,0000</strong></td>
            </tr>
          </tfoot>
        </table>
      </div>
    </section>
  </main>

  <footer class="footer">
    <small>v1.0 — Etapa inicial. Próximas regras serão adicionadas depois que você aprovar.</small>
  </footer>

  <script src="script.js"></script>
</body>
</html>
